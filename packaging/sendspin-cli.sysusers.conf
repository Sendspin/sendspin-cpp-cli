# The account sendspin-cli.service runs as; run `systemd-sysusers` once after installing.
# `m` adds it to audio, which /dev/snd (root:audio 0660) needs.
u sendspin-cli - "Sendspin audio player" - -
m sendspin-cli audio
